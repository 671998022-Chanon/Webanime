/**
 * Dev-only seed: populates genres, tags, and catalog relations.
 *
 * Run with:
 *   pnpm tsx tooling/scripts/seed-catalog.ts
 *
 * Stub for the M0 repository scaffold. Implementation belongs to the
 * catalog milestone (M2+) once the Drizzle schema is in place.
 */

console.log("[seed-catalog] not implemented yet — wired up during M2+ (catalog).");
