/**
 * Dev-only seed: creates an admin user and base roles.
 *
 * Run with:
 *   pnpm tsx tooling/scripts/seed-admin.ts
 *
 * This is intentionally a stub for the M0 repository scaffold.
 * Implementation (hashing passwords, inserting roles, idempotency)
 * belongs to the auth milestone (M3).
 */

console.log("[seed-admin] not implemented yet — wired up during M3 (auth).");
