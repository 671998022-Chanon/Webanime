// Genres section — Task 14.1 placeholder
// Receives a placeholder until genre pill wiring in Task 14.2.

import { PlaceholderContent, SectionShell } from "./section";

export function GenresSection() {
  return (
    <SectionShell id="genres" title="Genres">
      <PlaceholderContent label="Genres rail placeholder" />
    </SectionShell>
  );
}
