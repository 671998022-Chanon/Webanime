/* eslint-disable import/no-unresolved -- workspace link @nexus/ui resolves at build time */
// Re-export Button from @nexus/ui — the single source of truth.
// The shadcn starter has been replaced by the design-system implementation.
export { Button, buttonVariants } from "@nexus/ui";
